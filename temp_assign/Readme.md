# Car Price Preprocessing — Assignment 3

## Overview
This project cleans and preprocesses the `car_l3_dataset.csv` dataset to produce a model-ready dataset. Tasks include handling missing values, outliers, feature engineering, encoding, and scaling.

## Files

| File | Description |
|------|-------------|
| `l3_preprocess.py` | Script implementing all preprocessing steps and saves cleaned CSV. |
| `car_l3_clean_ready.csv` | Cleaned dataset ready for modeling. |
| `reflection.md` | Explains preprocessing decisions and logic. |
| `requirements.txt` | Exact package versions for reproducibility. |

## How to Run

1. Install packages:

```bash
pip install -r requirements.txt
